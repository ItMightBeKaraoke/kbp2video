[project]
name = "kbp2video"
version = "0.0.1"
dependencies = [
    "PySide6"
]
[project.scripts]
kbp2video = "kbp2video:run"
[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
[tool.setuptools.packages.find]
include = ["kbp2video"]
